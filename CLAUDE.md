# このリポジトリでの作業ルール

このプロジェクトでは以下のGit/GitHub運用ルールを**必ず**守ること。ユーザーから明示的に例外の指示がない限り、逸脱しない。

## 1. mainブランチへの直接コミット・pushは禁止

- `main`ブランチ上で直接コード変更・コミットを行わない。
- `main`へ直接 `git push` しない（GitHub側でもruleset（`protect-main`）により強制ブロックされる）。
- 作業を始める前に、必ず `main` から新しいブランチを作成すること。

## 2. 作業前に必ずIssueを作成する

- 何らかのタスク（機能追加・修正・調査など）に着手する前に、GitHub上にIssueを作成する。
- Issueには以下を含めること:
  - タイトル: 何をするかが分かる短い日本語
  - 本文: 背景・目的・完了条件（簡潔でよい）
- コマンド例: `gh issue create --title "..." --body "..."`

## 3. ブランチ命名規則

Issue番号を含めた以下の形式でブランチを作成する。

```
<type>/<issue番号>-<短い説明>
```

- `type` は以下のいずれか:
  - `feature` — 新機能
  - `fix` — バグ修正
  - `chore` — 雑務・依存更新・設定変更
  - `docs` — ドキュメントのみの変更
- 例: `feature/12-add-due-date-filter`, `fix/15-card-delete-bug`

コマンド例:
```
git checkout main
git pull
git checkout -b feature/12-add-due-date-filter
```

## 4. プルリクエスト運用

- 作業が完了したら、作成したブランチから `main` へのプルリクエストを作成する（`gh pr create`）。
- PR本文には対応するIssue番号を `Closes #12` の形式で必ず記載し、マージ時にIssueが自動クローズされるようにする。
- レビュー承認は0人でもマージ可能（個人開発のため）だが、PR経由でのマージを必須とする（直接pushは技術的にブロックされている）。
- マージ後、不要になったブランチは削除する。

## 5. Claude Codeとしての振る舞い

- ユーザーから「〜を実装して」「〜を直して」等の依頼を受けたら、着手前に上記フローに従い、Issue作成 → ブランチ作成 → 実装 → PR作成、の順で進める。
- 既にユーザーが別ブランチにいる、または明示的に「Issueは作らなくていい」「このブランチのまま進めて」等の指示がある場合はそれに従う。
- 破壊的操作（force push、ブランチ削除、`main`への直接操作など）を行う前は必ずユーザーに確認する。
